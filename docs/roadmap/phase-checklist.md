# 实施路线图与验收清单

本文档合并 `module-implementation-plan.md` 的 8 个 Phase 与 `config-orchestration-first-plan.md` 的 6 个 Phase，形成统一验收清单。

---

## 配置编排优先策略

本节内容提取自「配置编排优先实施方案」第 2 节，作为理解后续所有 Phase 决策的策略背景。

### 2.1 可控性更强

安装工具会执行本机命令。前期使用配置编排，可以让命令来源、执行顺序、失败处理和校验规则全部可审计、可测试。

### 2.2 上线风险更低

不依赖外部模型后，前期可以避免：

- API key 管理
- 模型调用失败
- 模型输出不稳定
- 调用成本控制
- 模型幻觉生成危险命令

### 2.3 更适合打底座

配置编排能力建好后，即使后续接入大模型，模型也只需要生成或补充配置，而不是直接参与执行。

---

## Phase 0：清理概念债务

**目标**：收束 Ollama 方向，明确配置编排优先路线。

### 任务
- [ ] 标记 `ai_assistant.rs` 为待替换
- [ ] 将 `ollama_endpoint` / `ollama_model` 从长期目标中移除
- [ ] README 不再把 Ollama 作为默认主路线

### 验收
- [ ] `npm run build` 成功
- [ ] `cargo check` 成功
- [ ] 文档层方向一致

---

## Phase 1：定义层重构

**目标**：扩展工具配置模型，支持官网链接、安装文档、生命周期配置和编排流程。

### 后端任务
- [ ] 新增 `ToolManifest` 及配套模型（Rust）
- [ ] 保持旧版 `Tool` 配置兼容

### 前端任务
- [ ] 同步前端类型拆分（`src/types/*.ts`）
- [ ] 工具列表展示文档来源与管理能力摘要
- [ ] 工具详情页展示官方链接、安装文档摘要、自动更新、自启状态

### 测试
- [ ] Rust：旧版工具配置可加载
- [ ] Rust：新版工具配置可加载
- [ ] Rust：缺失可选字段时有默认值
- [ ] Rust：非法步骤 ID 或空命令被拒绝
- [ ] TS：工具卡片/详情页展示逻辑测试

### 验收命令
```bash
cargo test --lib
cargo check
npm test
npm run build
```

---

## Phase 2：生命周期状态扩展

**目标**：补齐短期状态语义，同时保留现有 `ToolStatus` 兼容。

### 后端任务
- [ ] 增加 `Checking`
- [ ] 增加 `Planning`
- [ ] 增加 `NeedsReview`
- [ ] 增加 `Uninstalling`
- [ ] 增加 `Upgrading`
- [ ] 增加 `Diagnosing`
- [ ] 增加 `Repairing`

### 前端任务
- [ ] 更新 `StatusBadge` 颜色映射
- [ ] 更新 `ToolCard` 操作按钮策略
- [ ] 安装页支持 `Planning` 和 `NeedsReview`

### 测试
- [ ] Rust：状态持久化策略和状态切换
- [ ] Rust：非法状态跳转被拒绝或降级
- [ ] TS：状态标签、按钮可用性、安装页状态展示

---

## Phase 3：配置编排执行器

**目标**：让安装/卸载/升级/修复从命令数组升级为步骤流。

### 后端任务
- [ ] 新增 `orchestration.rs`
- [ ] 新增 `OrchestrationFlow`
- [ ] 新增 `OrchestrationStep`
- [ ] 支持条件判断
- [ ] 支持步骤级失败提示
- [ ] 支持 retry 策略
- [ ] 新增 `executor.rs`（从 `tool_manager.rs` 拆分）
- [ ] 新增 `policy.rs`（命令安全策略）

### 前端任务
- [ ] 安装页展示步骤流和阶段进度
- [ ] 工具详情页展示编排摘要
- [ ] 高风险配置步骤进入用户确认

### 测试
- [ ] Rust：条件命中时执行对应步骤
- [ ] Rust：条件不命中时跳过步骤
- [ ] Rust：required 步骤失败会中止流程
- [ ] Rust：optional 步骤失败会继续流程
- [ ] Rust：步骤流解析、计划校验、安全策略
- [ ] TS：步骤预览、风险展示、安装页阶段切换

---

## Phase 4：配置驱动升级与修复

**目标**：支持基于配置的升级、修复和失败建议。

### 后端任务
- [ ] 新增 `updater.rs`
- [ ] 支持 `upgrade` flow
- [ ] 支持 `repair` flow
- [ ] 支持步骤级失败建议和日志摘要
- [ ] 支持 `UpdateAvailable` 状态

### 前端任务
- [ ] 工具卡展示 `UpdateAvailable`
- [ ] 安装失败后展示配置建议
- [ ] 支持手动升级与修复入口

### 测试
- [ ] Rust：本地版本低于远程版本时返回 `UpdateAvailable`
- [ ] Rust：升级失败时保留原安装状态
- [ ] Rust：repair flow、失败收口
- [ ] TS：升级按钮、修复入口、失败建议展示

---

## Phase 5：工具级开机自启

**目标**：支持配置驱动的工具级开机自启。

### 后端任务
- [ ] 新增 `autostart.rs`
- [ ] 支持 `AppManaged` 模式
- [ ] 应用启动后根据配置拉起工具
- [ ] 自启失败写日志

### 前端任务
- [ ] 工具详情页增加自启开关
- [ ] 自启失败展示日志与建议

### 测试
- [ ] Rust：自启配置解析、待启动列表生成、禁用策略
- [ ] Rust：自启失败会记录日志
- [ ] TS：自启开关、状态展示

---

## Phase 6：日志摘要与失败建议

**目标**：不依赖模型，也能根据配置和日志给出失败建议。

### 后端任务
- [ ] 新增 `log_summary.rs`
- [ ] 支持错误片段提取
- [ ] 支持配置化失败建议匹配
- [ ] 支持统一失败摘要输出

### 前端任务
- [ ] 安装页展示失败摘要
- [ ] 展示配置化建议与文档链接

### 测试
- [ ] Rust：命中失败建议、未命中时展示通用建议
- [ ] Rust：日志摘要长度受控
- [ ] TS：失败建议展示、链接跳转

---

## Phase 7：外部模型增强层

**目标**：在配置编排底座稳定后，引入外部模型作为增强能力。

### 后端任务
- [ ] 新增 `model_client.rs`
- [ ] 新增结构化计划 schema
- [ ] 支持模型生成配置草案、升级草案、修复建议
- [ ] 所有模型输出必须写回配置层或计划层再执行

### 前端任务
- [ ] 设置页增加外部模型配置
- [ ] 安装页展示「模型建议」与「配置结果」的差异

### 测试
- [ ] Rust：模型请求构造、响应解析、计划校验
- [ ] TS：模型配置表单、建议展示

**详细计划参见**：[integration/model-integration-plan.md](integration/model-integration-plan.md)

---

## Phase 8：预算与审计

**目标**：控制调用成本并沉淀审计记录。

### 后端任务
- [ ] 新增 `budget.rs`
- [ ] 新增模型调用审计日志
- [ ] 支持单任务调用上限
- [ ] 支持全局调用上限

### 前端任务
- [ ] 安装页展示剩余调用次数或超限提示
- [ ] 设置页展示预算策略

### 测试
- [ ] Rust：预算扣减、超限拒绝、审计记录
- [ ] TS：超限提示与禁用状态

---

## 全局验收标准

每个 Phase 完成后必须通过：

```bash
# 后端
cargo check
cargo test --lib

# 前端
npm test
npm run build
```

## 优先级总览

| 优先级 | Phase | 名称 |
|--------|-------|------|
| P0 | Phase 0 | 清理概念债务 |
| P0 | Phase 1 | 定义层重构 |
| P0 | Phase 2 | 生命周期状态扩展 |
| P0 | Phase 3 | 配置编排执行器 |
| P1 | Phase 4 | 配置驱动升级与修复 |
| P1 | Phase 5 | 工具级开机自启 |
| P1 | Phase 6 | 日志摘要与失败建议 |
| P2 | Phase 7 | 外部模型增强层 |
| P2 | Phase 8 | 预算与审计 |
